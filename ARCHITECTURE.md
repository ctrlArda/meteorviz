# 🏗️ MeteorViz - Proje Çalışma Mimarisi

## 📊 Genel Bakış

MeteorViz, NASA Space Apps Challenge için geliştirilmiş bir **Asteroit Etki Simülasyonu ve Analiz Platformu**dur. Proje, **Hibrit Modelleme** yaklaşımını kullanarak fizik simülasyonları ile makine öğrenmesini birleştirir.

---

## 🔄 Sistem Akış Diyagramı

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              GELİŞTİRME AŞAMASI (Offline)                           │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│   ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐         │
│   │   NASA NeoWs     │      │   Fizik Motoru   │      │    ML Eğitimi    │         │
│   │   API'den Veri   │ ───► │   (Simülasyon)   │ ───► │  (train_model.py)│         │
│   │   Çekimi         │      │                  │      │                  │         │
│   └──────────────────┘      └──────────────────┘      └──────────────────┘         │
│           │                         │                         │                     │
│           │                         │                         │                     │
│           ▼                         ▼                         ▼                     │
│   ┌──────────────────┐      ┌──────────────────┐      ┌──────────────────┐         │
│   │ create_dataset_  │      │ nasa_impact_     │      │  impact_model.   │         │
│   │ from_nasa.py     │      │ dataset.csv      │      │  pkl             │         │
│   │ (2400+ asteroit) │      │ (Eğitim Verisi)  │      │  (AI Modeli)     │         │
│   └──────────────────┘      └──────────────────┘      └──────────────────┘         │
│                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        │ Model Dışa Aktarımı
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              ÇALIŞMA ZAMANI (Runtime)                               │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                     │
│   ┌──────────────────────────────────────────────────────────────────────────────┐ │
│   │                          BACKEND (Flask - app.py)                            │ │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │ │
│   │  │ NASA API    │  │ Fizik       │  │ ML Model    │  │ GIS         │         │ │
│   │  │ Entegrasyonu│  │ Simülasyonu │  │ (Tahmin)    │  │ (Nüfus/DEM) │         │ │
│   │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘         │ │
│   │         │                │                │                │                 │ │
│   │         └────────────────┴────────────────┴────────────────┘                 │ │
│   │                                   │                                          │ │
│   │                           REST API Endpoints                                 │ │
│   │                    /predict, /lookup_asteroid, /monte_carlo                  │ │
│   └──────────────────────────────────────────────────────────────────────────────┘ │
│                                        │                                           │
│                                        │ JSON Response                             │
│                                        ▼                                           │
│   ┌──────────────────────────────────────────────────────────────────────────────┐ │
│   │                      FRONTEND (HTML/CSS/JavaScript)                          │ │
│   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │ │
│   │  │ index.html  │  │ style.css   │  │simulation_  │  │ Leaflet.js  │         │ │
│   │  │ (Arayüz)    │  │ (Tasarım)   │  │v2.js (Mantık│  │ (Harita)    │         │ │
│   │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘         │ │
│   └──────────────────────────────────────────────────────────────────────────────┘ │
│                                        │                                           │
│                                        ▼                                           │
│                              ┌──────────────────┐                                  │
│                              │   KULLANICI      │                                  │
│                              │   (Web Tarayıcı) │                                  │
│                              └──────────────────┘                                  │
│                                                                                     │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 Dosya Yapısı ve Sorumlulukları

### 🔵 VERİ KATMANI (Data Layer)

| Dosya | Açıklama |
|-------|----------|
| `create_dataset_from_nasa.py` | NASA NeoWs API'den gerçek asteroit verilerini çeker, fizik simülasyonları ile zenginleştirir ve `nasa_impact_dataset.csv` dosyasını oluşturur |
| `nasa_impact_dataset.csv` | ~2400+ asteroit verisi içeren eğitim veri seti (çap, hız, kütle, bileşim, krater çapı vb.) |

### 🟢 MAKİNE ÖĞRENMESİ KATMANI (ML Layer)

| Dosya | Açıklama |
|-------|----------|
| `train_model.py` | Gradient Boosting modeli eğitir, cross-validation yapar, `impact_model.pkl` dosyasını oluşturur |
| `validate_model.py` | Eğitilen modeli fizik simülasyonları ile karşılaştırarak doğrular |
| `impact_model.pkl` | Eğitilmiş makine öğrenmesi modeli (üretilen) |

### 🟠 BACKEND KATMANI (API Layer)

| Dosya | Açıklama |
|-------|----------|
| `app.py` | Flask REST API sunucusu - Tüm iş mantığını içerir |

**Ana Endpoint'ler:**
- `/predict` - Çarpışma simülasyonu ve etki tahmini
- `/lookup_asteroid/<id>` - NASA API'den asteroit detayları
- `/monte_carlo` - Olasılıksal analiz (1000+ simülasyon)
- `/get_dataset_asteroids` - Kayıtlı asteroit listesi
- `/validate_physics` - Fizik modeli doğrulama

### 🔴 FRONTEND KATMANI (UI Layer)

| Dosya | Açıklama |
|-------|----------|
| `index.html` | Ana web sayfası yapısı (Tailwind CSS ile) |
| `style.css` | Özel CSS stilleri |
| `simulation_v2.js` | Tüm frontend JavaScript mantığı |

**Frontend Özellikleri:**
- Leaflet.js ile interaktif harita
- Asteroit arama ve seçim
- Simülasyon sonuçlarının görselleştirilmesi
- Monte Carlo analizi arayüzü

### 🟣 GÖRSELLEŞTİRME KATMANI (Visualization Layer)

| Dosya | Açıklama |
|-------|----------|
| `make_visuals.py` | Genel görselleştirmeler |
| `make_model_visuals.py` | Model performans grafikleri |
| `make_jury_visuals.py` | Jüri sunumu için infografikler |
| `make_simulation_visuals.py` | Simülasyon grafikleri |
| `make_architecture_diagram.py` | Mimari diyagramı |
| `results/` | Tüm üretilen görseller |

---

## 🔬 Fizik Motoru Detayları

### Kullanılan Bilimsel Modeller

```
┌─────────────────────────────────────────────────────────────────┐
│                    ATMOSFERİK GİRİŞ MODELİ                      │
├─────────────────────────────────────────────────────────────────┤
│  1. Sürüklenme (Drag) Hesabı                                    │
│     F_drag = 0.5 * ρ_air * v² * C_d * A                         │
│                                                                  │
│  2. Ablasyon (Kütle Kaybı)                                      │
│     dm/dt = -C_h * ρ_air * v³ * A / (2 * Q)                     │
│                                                                  │
│  3. Pancake Effect (Yassılaşma)                                 │
│     dL/dt = v * sqrt(ρ_air / ρ_asteroid) * L / L₀               │
│     Kaynak: Chyba et al. (1993), Collins et al. (2005)          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    KRATER OLUŞUM MODELİ                         │
├─────────────────────────────────────────────────────────────────┤
│  Krater Çapı:                                                    │
│  D = 1.161 * (ρ_imp/ρ_target)^(1/3) * (E/(g*ρ_target))^0.25    │
│                              * sin(θ)^(1/3)                      │
│                                                                  │
│  Kaynak: Pi-Scaling Laws (Holsapple & Schmidt)                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ETKİ YARICAPLARI HESABI                      │
├─────────────────────────────────────────────────────────────────┤
│  • Ateş Topu Yarıçapı (Fireball)                                │
│  • Termal Radyasyon Yarıçapı                                    │
│  • Şok Dalgası Yarıçapı (1psi, 4psi, 10psi basınç)              │
│  • Deprem Etkisi (Richter ölçeği)                               │
│  • Tsunami Modeli (okyanus çarpışmaları için)                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🤖 Makine Öğrenmesi Pipeline

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   HAM VERİ   │     │   ÖZELLİK    │     │    MODEL     │     │   TAHMİN     │
│              │────►│  MÜHENDİSLİĞİ│────►│   EĞİTİMİ    │────►│   ÇIKIŞI     │
└──────────────┘     └──────────────┘     └──────────────┘     └──────────────┘
       │                    │                    │                    │
       │                    │                    │                    │
       ▼                    ▼                    ▼                    ▼
  - Çap (m)           - log(kütle)         Gradient         - Krater Çapı
  - Hız (km/s)        - momentum           Boosting         - Enerji (J)
  - Kütle (kg)        - One-Hot Encoding   Regressor        - Etki Yarıçapları
  - Bileşim           - is_hazardous       (R² > 0.95)
  - Açı (derece)
```

### Model Performansı
- **Algoritma:** Gradient Boosting Regressor
- **Cross-Validation:** 5-Fold
- **Metrikler:** R², MAE, RMSE
- **Özellik Önem Sıralaması:** `results/feature_importance.png`

---

## 🌐 API Akış Şeması

```
┌─────────────┐                    ┌─────────────┐                    ┌─────────────┐
│  KULLANICI  │                    │   FLASK     │                    │   NASA      │
│  (Frontend) │                    │   BACKEND   │                    │   API       │
└──────┬──────┘                    └──────┬──────┘                    └──────┬──────┘
       │                                  │                                  │
       │  1. Asteroit Ara                 │                                  │
       │─────────────────────────────────►│                                  │
       │                                  │  2. NeoWs API Sorgusu            │
       │                                  │─────────────────────────────────►│
       │                                  │                                  │
       │                                  │  3. Asteroit Verileri            │
       │                                  │◄─────────────────────────────────│
       │                                  │                                  │
       │  4. Asteroit Detayları (JSON)    │                                  │
       │◄─────────────────────────────────│                                  │
       │                                  │                                  │
       │  5. Simülasyon Başlat            │                                  │
       │─────────────────────────────────►│                                  │
       │                                  │                                  │
       │                          ┌───────┴───────┐                          │
       │                          │  HİBRİT MOTOR │                          │
       │                          │  • Fizik Sim. │                          │
       │                          │  • ML Tahmin  │                          │
       │                          │  • GIS Analiz │                          │
       │                          └───────┬───────┘                          │
       │                                  │                                  │
       │  6. Simülasyon Sonuçları (JSON)  │                                  │
       │◄─────────────────────────────────│                                  │
       │                                  │                                  │
       │  7. Haritada Görselleştir        │                                  │
       │  (Leaflet.js)                    │                                  │
       │                                  │                                  │
```

---

## 🔧 Harici Bağımlılıklar

### Python Kütüphaneleri
```
Flask, Flask-CORS     → Web sunucusu
NumPy, Pandas         → Veri işleme
Scikit-learn          → Makine öğrenmesi
Rasterio, GeoPandas   → GIS işlemleri
Matplotlib, Seaborn   → Görselleştirme
Requests              → API istekleri
Joblib                → Model serileştirme
```

### JavaScript Kütüphaneleri
```
Leaflet.js            → İnteraktif harita
Tailwind CSS          → UI styling
```

### Harici Veri Kaynakları
```
NASA NeoWs API        → Gerçek zamanlı asteroit verileri
WorldPop Data         → Dünya nüfus yoğunluğu (GeoTIFF)
DEM/Bathymetry        → Yükseklik/derinlik verileri (opsiyonel)
```

---

## 🚀 Çalıştırma Sırası

```bash
# 1. Veri setini oluştur (ilk kurulumda bir kez)
python create_dataset_from_nasa.py

# 2. ML modelini eğit (ilk kurulumda bir kez)
python train_model.py

# 3. (Opsiyonel) Modeli doğrula
python validate_model.py

# 4. Backend sunucusunu başlat
python app.py

# 5. Tarayıcıda aç
# http://127.0.0.1:5001
```

---

## 📈 Performans Optimizasyonları

| Bileşen | Optimizasyon |
|---------|--------------|
| WorldPop Verisi | Uygulama başlangıcında belleğe yüklenir |
| ML Modeli | `.pkl` formatında serileştirilir |
| API Yanıtları | JSON formatında minimal veri transferi |
| Fizik Hesapları | NumPy vektörizasyonu ile hızlandırılır |

---

## 🎯 Hibrit Yaklaşımın Avantajları

```
┌──────────────────────┐          ┌──────────────────────┐
│    FİZİK MOTORU      │          │    ML MODELİ         │
│    ✓ Doğru           │    +     │    ✓ Hızlı           │
│    ✗ Yavaş           │    =     │    ✗ Kara kutu       │
└──────────────────────┘          └──────────────────────┘
                    │
                    ▼
          ┌──────────────────────┐
          │   HİBRİT SİSTEM      │
          │   ✓ Doğru            │
          │   ✓ Hızlı            │
          │   ✓ Açıklanabilir    │
          └──────────────────────┘
```

**Sonuç:** Fizik motorunun doğruluğu + ML'in hızı = Gerçek zamanlı, bilimsel simülasyon
